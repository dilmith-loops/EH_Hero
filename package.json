{
  "name": "virtual-try-on",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "cp index.dev.html index.html && vite build && cp dist/index.html index.html && rm -rf ./assets && cp -r dist/assets ./assets && cp dist/*.jpeg dist/*.png . 2>/dev/null || true",
    "preview": "vite preview"
  },
  "dependencies": {
    "@google/genai": "^1.35.0",
    "@gradio/client": "^2.2.1",
    "buffer": "^6.0.3",
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
